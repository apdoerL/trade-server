spring:
  cloud:
    config:
      name: trade-server
      profile: dev
      label: dev
      discovery:
        enabled: true
        service-id: config-server

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka


